/** @type {import('next').NextConfig} */
const repo = "concept-row-scroll-system-";

export default {
  output: "export",
  images: { unoptimized: true },
  basePath: `/${repo}`,     
  assetPrefix: `/${repo}/`, 
};
